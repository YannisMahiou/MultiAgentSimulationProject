package Model.Terrain;

/**
 * Terrain sub class used in our SMA
 */
public class Terrain extends AbstractTerrain {

    /**
     * Terrain Constructor
     *
     * @param sizeX the X size of the terrain
     * @param sizeY the Y size of the terrain
     */
    public Terrain(int sizeX, int sizeY) {
        super(sizeX, sizeY);
    }
}
